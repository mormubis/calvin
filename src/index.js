import SVG from './SVG';
import { Arc, Area, Circle, Gradient, Line, Rect, Stop } from './Shapes';

export { default as Animation } from './Animation';
export { default as Layer } from './Layer';

export { Arc, Area, Circle, Gradient, Line, Rect, SVG, Stop };

export const Shapes = { Arc, Area, Circle, Gradient, Line, Rect, Stop };

export default SVG;
