import SVG from './SVG';
import {
  Arc,
  Area,
  Circle,
  Gradient,
  Line,
  Polygon,
  Rect,
  Stop,
  Text,
} from './Shapes';

export { default as Animation } from './Animation';
export { default as Layer } from './Layer';

export { Arc, Area, Circle, Gradient, Line, Polygon, Rect, SVG, Stop, Text };

export const Shapes = {
  Arc,
  Area,
  Circle,
  Gradient,
  Line,
  Polygon,
  Rect,
  Stop,
  Text,
};

export default SVG;
